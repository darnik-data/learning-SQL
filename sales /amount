/* вывести количество покупок в каждой категории, где покупок было больше одной */

SELECT category, 
COUNT(amount) AS quantity
FROM sales
GROUP BY category
HAVING COUNT(amount) > 1
ORDER BY quantity DESC;
